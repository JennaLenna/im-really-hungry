{
  "name": "im-really-hungry",
  "version": "1.0.0",
  "description": "This small demo renders a pixel-art title screen using an offscreen low-resolution buffer that gets scaled up with nearest-neighbor interpolation so the visuals stay blocky/pixelated.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^22.15.0"
  }
}
